#include "UIElement.h"
#include "../Renderer.h"

/*
void UIElement::Draw()
{
	Renderer::DrawQuad({ m_location.x + m_size.x / 2, m_location.y + m_size.y / 2, m_location.z }, m_size, m_colour);
}



void TextBox::Draw()
{
	
	//Renderer::RenderText({ m_location.x + ((m_size.x * m_textSize) / 2.0f), m_location.y + ((m_size.y * m_textSize) / 2.0f), 0.0f }, m_size, m_text, m_font);
	Renderer::RenderText({ m_location.x, m_location.y, m_location.z - 0.01f }, 
		{ m_size.y * m_textSize / m_font->GetPtSize(), 
		m_size.y * m_textSize / m_font->GetPtSize() }, 
		m_text, m_font, m_textColour);
	Renderer::DrawQuad({ m_location.x + m_size.x / 2.0f, m_location.y + m_size.y / 2.0f, m_location.z }, m_size, m_colour);
}
*/
